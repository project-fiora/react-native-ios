/**
 * Created by kusob on 2017. 6. 27..
 */

import { AppRegistry } from 'react-native';

import project_fiora from './root';

console.ignoredYellowBox = ['Remote debugger'];

AppRegistry.registerComponent('project_fiora', () => project_fiora);
